import { customers, enrished_customer } from './customers.controller';
import { accountsByCustomer, allAccounts } from './accounts.controller';
import { transactions } from './transactions.controller';

export default {
    customers,
    enrished_customer,
    transactions,
    allAccounts,
    accountsByCustomer,
};
